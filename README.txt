Fuel Placement Problem

fuel_placement.py :
    contains the main class
    contains several algorithms for solving instances of the problem

fuel_placement_tests.py :
    contains several tests
    contains several searches for bad instances of the problem
    with the exception of the search_max_min_UB, no searches have succeeded
    the search parameters may be tuned
